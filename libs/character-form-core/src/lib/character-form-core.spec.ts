import { characterFormCore } from './character-form-core';

describe('characterFormCore', () => {
  it('should work', () => {
    expect(characterFormCore()).toEqual('character-form-core');
  });
});
